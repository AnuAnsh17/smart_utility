# Frontend configuration.
#
# Copy to .env.local only if the backend is not on the default loopback
# address. Anything other than 127.0.0.1/localhost means bill images leave
# this machine, which is almost never what you want.

NEXT_PUBLIC_API_BASE="http://127.0.0.1:8000/api/v1"
